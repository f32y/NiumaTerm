# Local-dev-only token, shared with the ignored Rust integration test
# (crates/remote_net/tests/relay_integration.rs). Production tokens are set
# via `wrangler secret put ACCESS_TOKEN` and never live in the repo.
ACCESS_TOKEN=test-token
