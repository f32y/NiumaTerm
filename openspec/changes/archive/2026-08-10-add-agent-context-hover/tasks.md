## 1. Context Accounting Model

- [x] 1.1 Extend the shared context snapshot with optional token categories and explicitly scoped cumulative usage.
- [x] 1.2 Parse Codex current and thread-total token breakdowns with focused compatibility tests.
- [x] 1.3 Parse Claude current and last-turn breakdowns, including post-compaction sparse snapshots, with focused tests.

## 2. Hover Presentation

- [x] 2.1 Add tested formatting helpers for compact totals, remaining capacity, conditional rows, and cumulative headings.
- [x] 2.2 Wrap the existing composer context indicator in a themed Agent Context hover card with current and cumulative sections.

## 3. Validation

- [x] 3.1 Run formatting, focused tests, workspace checks, and a testing-mode visual validation of the hover behavior.
